import {
  __commonJS,
  __publicField,
  require_react
} from "./chunk-IWFXIWQ7.js";

// node_modules/telepath-unpack/lib/index.js
var require_lib = __commonJS({
  "node_modules/telepath-unpack/lib/index.js"(exports, module) {
    "use strict";
    function _construct(Parent, args, Class) {
      if (_isNativeReflectConstruct()) {
        _construct = Reflect.construct;
      } else {
        _construct = function _construct2(Parent2, args2, Class2) {
          var a = [null];
          a.push.apply(a, args2);
          var Constructor = Function.bind.apply(Parent2, a);
          var instance = new Constructor();
          if (Class2) _setPrototypeOf(instance, Class2.prototype);
          return instance;
        };
      }
      return _construct.apply(null, arguments);
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _setPrototypeOf(o, p) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o2, p2) {
        o2.__proto__ = p2;
        return o2;
      };
      return _setPrototypeOf(o, p);
    }
    function _toConsumableArray(arr) {
      return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
    }
    function _nonIterableSpread() {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _iterableToArray(iter) {
      if (typeof Symbol !== "undefined" && Symbol.iterator in Object(iter)) return Array.from(iter);
    }
    function _arrayWithoutHoles(arr) {
      if (Array.isArray(arr)) return _arrayLikeToArray(arr);
    }
    function _slicedToArray(arr, i) {
      return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest();
    }
    function _nonIterableRest() {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _unsupportedIterableToArray(o, minLen) {
      if (!o) return;
      if (typeof o === "string") return _arrayLikeToArray(o, minLen);
      var n = Object.prototype.toString.call(o).slice(8, -1);
      if (n === "Object" && o.constructor) n = o.constructor.name;
      if (n === "Map" || n === "Set") return Array.from(o);
      if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
    }
    function _arrayLikeToArray(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i = 0, arr2 = new Array(len); i < len; i++) {
        arr2[i] = arr[i];
      }
      return arr2;
    }
    function _iterableToArrayLimit(arr, i) {
      if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return;
      var _arr = [];
      var _n = true;
      var _d = false;
      var _e = void 0;
      try {
        for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
          _arr.push(_s.value);
          if (i && _arr.length === i) break;
        }
      } catch (err) {
        _d = true;
        _e = err;
      } finally {
        try {
          if (!_n && _i["return"] != null) _i["return"]();
        } finally {
          if (_d) throw _e;
        }
      }
      return _arr;
    }
    function _arrayWithHoles(arr) {
      if (Array.isArray(arr)) return arr;
    }
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i = 0; i < props.length; i++) {
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    var Telepath = function() {
      function Telepath2() {
        _classCallCheck(this, Telepath2);
        this.constructors = {};
      }
      _createClass(Telepath2, [{
        key: "register",
        value: function register(name, constructor) {
          this.constructors[name] = constructor;
        }
      }, {
        key: "unpack",
        value: function unpack(objData) {
          var packedValuesById = {};
          this.scanForIds(objData, packedValuesById);
          var valuesById = {};
          return this.unpackWithRefs(objData, packedValuesById, valuesById);
        }
      }, {
        key: "scanForIds",
        value: function scanForIds(objData, packedValuesById) {
          var _this = this;
          if (objData === null || _typeof(objData) !== "object") {
            return;
          }
          if (Array.isArray(objData)) {
            objData.forEach(function(item) {
              return _this.scanForIds(item, packedValuesById);
            });
            return;
          }
          var hasReservedKeyNames = false;
          if ("_id" in objData) {
            hasReservedKeyNames = true;
            packedValuesById[objData["_id"]] = objData;
          }
          if ("_type" in objData || "_val" in objData || "_ref" in objData) {
            hasReservedKeyNames = true;
          }
          if ("_list" in objData) {
            hasReservedKeyNames = true;
            objData["_list"].forEach(function(item) {
              return _this.scanForIds(item, packedValuesById);
            });
          }
          if ("_args" in objData) {
            hasReservedKeyNames = true;
            objData["_args"].forEach(function(item) {
              return _this.scanForIds(item, packedValuesById);
            });
          }
          if ("_dict" in objData) {
            hasReservedKeyNames = true;
            for (var _i = 0, _Object$entries = Object.entries(objData["_dict"]); _i < _Object$entries.length; _i++) {
              var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2), key = _Object$entries$_i[0], val = _Object$entries$_i[1];
              this.scanForIds(val, packedValuesById);
            }
          }
          if (!hasReservedKeyNames) {
            for (var _i2 = 0, _Object$entries2 = Object.entries(objData); _i2 < _Object$entries2.length; _i2++) {
              var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2), _key = _Object$entries2$_i[0], _val = _Object$entries2$_i[1];
              this.scanForIds(_val, packedValuesById);
            }
          }
        }
      }, {
        key: "unpackWithRefs",
        value: function unpackWithRefs(objData, packedValuesById, valuesById) {
          var _this2 = this;
          if (objData === null || _typeof(objData) !== "object") {
            return objData;
          }
          if (Array.isArray(objData)) {
            return objData.map(function(item) {
              return _this2.unpackWithRefs(item, packedValuesById, valuesById);
            });
          }
          var result;
          if ("_ref" in objData) {
            if (objData["_ref"] in valuesById) {
              result = valuesById[objData["_ref"]];
            } else {
              result = this.unpackWithRefs(packedValuesById[objData["_ref"]], packedValuesById, valuesById);
            }
          } else if ("_val" in objData) {
            result = objData["_val"];
          } else if ("_list" in objData) {
            result = objData["_list"].map(function(item) {
              return _this2.unpackWithRefs(item, packedValuesById, valuesById);
            });
          } else if ("_dict" in objData) {
            result = {};
            for (var _i3 = 0, _Object$entries3 = Object.entries(objData["_dict"]); _i3 < _Object$entries3.length; _i3++) {
              var _Object$entries3$_i = _slicedToArray(_Object$entries3[_i3], 2), key = _Object$entries3$_i[0], val = _Object$entries3$_i[1];
              result[key] = this.unpackWithRefs(val, packedValuesById, valuesById);
            }
          } else if ("_type" in objData) {
            var constructorId = objData["_type"];
            var _constructor = this.constructors[constructorId];
            if (typeof _constructor !== "function") {
              throw new Error("telepath encountered unknown object type ".concat(constructorId));
            }
            var args = objData["_args"].map(function(arg) {
              return _this2.unpackWithRefs(arg, packedValuesById, valuesById);
            });
            result = _construct(_constructor, _toConsumableArray(args));
          } else if ("_id" in objData) {
            throw new Error("telepath encountered object with _id but no type specified");
          } else {
            result = {};
            for (var _i4 = 0, _Object$entries4 = Object.entries(objData); _i4 < _Object$entries4.length; _i4++) {
              var _Object$entries4$_i = _slicedToArray(_Object$entries4[_i4], 2), _key2 = _Object$entries4$_i[0], _val2 = _Object$entries4$_i[1];
              result[_key2] = this.unpackWithRefs(_val2, packedValuesById, valuesById);
            }
            return result;
          }
          if ("_id" in objData) {
            valuesById[objData["_id"]] = result;
          }
          return result;
        }
      }]);
      return Telepath2;
    }();
    module.exports = Telepath;
  }
});

// node_modules/react/cjs/react-jsx-runtime.development.js
var require_react_jsx_runtime_development = __commonJS({
  "node_modules/react/cjs/react-jsx-runtime.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var React = require_react();
        var REACT_ELEMENT_TYPE = Symbol.for("react.element");
        var REACT_PORTAL_TYPE = Symbol.for("react.portal");
        var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
        var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
        var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
        var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
        var REACT_CONTEXT_TYPE = Symbol.for("react.context");
        var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
        var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
        var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
        var REACT_MEMO_TYPE = Symbol.for("react.memo");
        var REACT_LAZY_TYPE = Symbol.for("react.lazy");
        var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
        var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
        var FAUX_ITERATOR_SYMBOL = "@@iterator";
        function getIteratorFn(maybeIterable) {
          if (maybeIterable === null || typeof maybeIterable !== "object") {
            return null;
          }
          var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
          if (typeof maybeIterator === "function") {
            return maybeIterator;
          }
          return null;
        }
        var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
        function error(format) {
          {
            {
              for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                args[_key2 - 1] = arguments[_key2];
              }
              printWarning("error", format, args);
            }
          }
        }
        function printWarning(level, format, args) {
          {
            var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame;
            var stack = ReactDebugCurrentFrame2.getStackAddendum();
            if (stack !== "") {
              format += "%s";
              args = args.concat([stack]);
            }
            var argsWithFormat = args.map(function(item) {
              return String(item);
            });
            argsWithFormat.unshift("Warning: " + format);
            Function.prototype.apply.call(console[level], console, argsWithFormat);
          }
        }
        var enableScopeAPI = false;
        var enableCacheElement = false;
        var enableTransitionTracing = false;
        var enableLegacyHidden = false;
        var enableDebugTracing = false;
        var REACT_MODULE_REFERENCE;
        {
          REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
        }
        function isValidElementType(type) {
          if (typeof type === "string" || typeof type === "function") {
            return true;
          }
          if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) {
            return true;
          }
          if (typeof type === "object" && type !== null) {
            if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
            // types supported by any Flight configuration anywhere since
            // we don't know which Flight build this will end up being used
            // with.
            type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) {
              return true;
            }
          }
          return false;
        }
        function getWrappedName(outerType, innerType, wrapperName) {
          var displayName = outerType.displayName;
          if (displayName) {
            return displayName;
          }
          var functionName = innerType.displayName || innerType.name || "";
          return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
        }
        function getContextName(type) {
          return type.displayName || "Context";
        }
        function getComponentNameFromType(type) {
          if (type == null) {
            return null;
          }
          {
            if (typeof type.tag === "number") {
              error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
            }
          }
          if (typeof type === "function") {
            return type.displayName || type.name || null;
          }
          if (typeof type === "string") {
            return type;
          }
          switch (type) {
            case REACT_FRAGMENT_TYPE:
              return "Fragment";
            case REACT_PORTAL_TYPE:
              return "Portal";
            case REACT_PROFILER_TYPE:
              return "Profiler";
            case REACT_STRICT_MODE_TYPE:
              return "StrictMode";
            case REACT_SUSPENSE_TYPE:
              return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
              return "SuspenseList";
          }
          if (typeof type === "object") {
            switch (type.$$typeof) {
              case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + ".Consumer";
              case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + ".Provider";
              case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, "ForwardRef");
              case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                if (outerName !== null) {
                  return outerName;
                }
                return getComponentNameFromType(type.type) || "Memo";
              case REACT_LAZY_TYPE: {
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                  return getComponentNameFromType(init(payload));
                } catch (x) {
                  return null;
                }
              }
            }
          }
          return null;
        }
        var assign = Object.assign;
        var disabledDepth = 0;
        var prevLog;
        var prevInfo;
        var prevWarn;
        var prevError;
        var prevGroup;
        var prevGroupCollapsed;
        var prevGroupEnd;
        function disabledLog() {
        }
        disabledLog.__reactDisabledLog = true;
        function disableLogs() {
          {
            if (disabledDepth === 0) {
              prevLog = console.log;
              prevInfo = console.info;
              prevWarn = console.warn;
              prevError = console.error;
              prevGroup = console.group;
              prevGroupCollapsed = console.groupCollapsed;
              prevGroupEnd = console.groupEnd;
              var props = {
                configurable: true,
                enumerable: true,
                value: disabledLog,
                writable: true
              };
              Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
              });
            }
            disabledDepth++;
          }
        }
        function reenableLogs() {
          {
            disabledDepth--;
            if (disabledDepth === 0) {
              var props = {
                configurable: true,
                enumerable: true,
                writable: true
              };
              Object.defineProperties(console, {
                log: assign({}, props, {
                  value: prevLog
                }),
                info: assign({}, props, {
                  value: prevInfo
                }),
                warn: assign({}, props, {
                  value: prevWarn
                }),
                error: assign({}, props, {
                  value: prevError
                }),
                group: assign({}, props, {
                  value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                  value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                  value: prevGroupEnd
                })
              });
            }
            if (disabledDepth < 0) {
              error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
            }
          }
        }
        var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
        var prefix;
        function describeBuiltInComponentFrame(name, source, ownerFn) {
          {
            if (prefix === void 0) {
              try {
                throw Error();
              } catch (x) {
                var match = x.stack.trim().match(/\n( *(at )?)/);
                prefix = match && match[1] || "";
              }
            }
            return "\n" + prefix + name;
          }
        }
        var reentry = false;
        var componentFrameCache;
        {
          var PossiblyWeakMap = typeof WeakMap === "function" ? WeakMap : Map;
          componentFrameCache = new PossiblyWeakMap();
        }
        function describeNativeComponentFrame(fn, construct) {
          if (!fn || reentry) {
            return "";
          }
          {
            var frame = componentFrameCache.get(fn);
            if (frame !== void 0) {
              return frame;
            }
          }
          var control;
          reentry = true;
          var previousPrepareStackTrace = Error.prepareStackTrace;
          Error.prepareStackTrace = void 0;
          var previousDispatcher;
          {
            previousDispatcher = ReactCurrentDispatcher.current;
            ReactCurrentDispatcher.current = null;
            disableLogs();
          }
          try {
            if (construct) {
              var Fake = function() {
                throw Error();
              };
              Object.defineProperty(Fake.prototype, "props", {
                set: function() {
                  throw Error();
                }
              });
              if (typeof Reflect === "object" && Reflect.construct) {
                try {
                  Reflect.construct(Fake, []);
                } catch (x) {
                  control = x;
                }
                Reflect.construct(fn, [], Fake);
              } else {
                try {
                  Fake.call();
                } catch (x) {
                  control = x;
                }
                fn.call(Fake.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (x) {
                control = x;
              }
              fn();
            }
          } catch (sample) {
            if (sample && control && typeof sample.stack === "string") {
              var sampleLines = sample.stack.split("\n");
              var controlLines = control.stack.split("\n");
              var s = sampleLines.length - 1;
              var c = controlLines.length - 1;
              while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
                c--;
              }
              for (; s >= 1 && c >= 0; s--, c--) {
                if (sampleLines[s] !== controlLines[c]) {
                  if (s !== 1 || c !== 1) {
                    do {
                      s--;
                      c--;
                      if (c < 0 || sampleLines[s] !== controlLines[c]) {
                        var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
                        if (fn.displayName && _frame.includes("<anonymous>")) {
                          _frame = _frame.replace("<anonymous>", fn.displayName);
                        }
                        {
                          if (typeof fn === "function") {
                            componentFrameCache.set(fn, _frame);
                          }
                        }
                        return _frame;
                      }
                    } while (s >= 1 && c >= 0);
                  }
                  break;
                }
              }
            }
          } finally {
            reentry = false;
            {
              ReactCurrentDispatcher.current = previousDispatcher;
              reenableLogs();
            }
            Error.prepareStackTrace = previousPrepareStackTrace;
          }
          var name = fn ? fn.displayName || fn.name : "";
          var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
          {
            if (typeof fn === "function") {
              componentFrameCache.set(fn, syntheticFrame);
            }
          }
          return syntheticFrame;
        }
        function describeFunctionComponentFrame(fn, source, ownerFn) {
          {
            return describeNativeComponentFrame(fn, false);
          }
        }
        function shouldConstruct(Component) {
          var prototype = Component.prototype;
          return !!(prototype && prototype.isReactComponent);
        }
        function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
          if (type == null) {
            return "";
          }
          if (typeof type === "function") {
            {
              return describeNativeComponentFrame(type, shouldConstruct(type));
            }
          }
          if (typeof type === "string") {
            return describeBuiltInComponentFrame(type);
          }
          switch (type) {
            case REACT_SUSPENSE_TYPE:
              return describeBuiltInComponentFrame("Suspense");
            case REACT_SUSPENSE_LIST_TYPE:
              return describeBuiltInComponentFrame("SuspenseList");
          }
          if (typeof type === "object") {
            switch (type.$$typeof) {
              case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
              case REACT_MEMO_TYPE:
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
              case REACT_LAZY_TYPE: {
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                  return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch (x) {
                }
              }
            }
          }
          return "";
        }
        var hasOwnProperty = Object.prototype.hasOwnProperty;
        var loggedTypeFailures = {};
        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement(element) {
          {
            if (element) {
              var owner = element._owner;
              var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
              ReactDebugCurrentFrame.setExtraStackFrame(stack);
            } else {
              ReactDebugCurrentFrame.setExtraStackFrame(null);
            }
          }
        }
        function checkPropTypes(typeSpecs, values, location, componentName, element) {
          {
            var has = Function.call.bind(hasOwnProperty);
            for (var typeSpecName in typeSpecs) {
              if (has(typeSpecs, typeSpecName)) {
                var error$1 = void 0;
                try {
                  if (typeof typeSpecs[typeSpecName] !== "function") {
                    var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                    err.name = "Invariant Violation";
                    throw err;
                  }
                  error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                } catch (ex) {
                  error$1 = ex;
                }
                if (error$1 && !(error$1 instanceof Error)) {
                  setCurrentlyValidatingElement(element);
                  error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
                  setCurrentlyValidatingElement(null);
                }
                if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                  loggedTypeFailures[error$1.message] = true;
                  setCurrentlyValidatingElement(element);
                  error("Failed %s type: %s", location, error$1.message);
                  setCurrentlyValidatingElement(null);
                }
              }
            }
          }
        }
        var isArrayImpl = Array.isArray;
        function isArray(a) {
          return isArrayImpl(a);
        }
        function typeName(value) {
          {
            var hasToStringTag = typeof Symbol === "function" && Symbol.toStringTag;
            var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            return type;
          }
        }
        function willCoercionThrow(value) {
          {
            try {
              testStringCoercion(value);
              return false;
            } catch (e) {
              return true;
            }
          }
        }
        function testStringCoercion(value) {
          return "" + value;
        }
        function checkKeyStringCoercion(value) {
          {
            if (willCoercionThrow(value)) {
              error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
              return testStringCoercion(value);
            }
          }
        }
        var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
        var RESERVED_PROPS = {
          key: true,
          ref: true,
          __self: true,
          __source: true
        };
        var specialPropKeyWarningShown;
        var specialPropRefWarningShown;
        var didWarnAboutStringRefs;
        {
          didWarnAboutStringRefs = {};
        }
        function hasValidRef(config) {
          {
            if (hasOwnProperty.call(config, "ref")) {
              var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
              if (getter && getter.isReactWarning) {
                return false;
              }
            }
          }
          return config.ref !== void 0;
        }
        function hasValidKey(config) {
          {
            if (hasOwnProperty.call(config, "key")) {
              var getter = Object.getOwnPropertyDescriptor(config, "key").get;
              if (getter && getter.isReactWarning) {
                return false;
              }
            }
          }
          return config.key !== void 0;
        }
        function warnIfStringRefCannotBeAutoConverted(config, self) {
          {
            if (typeof config.ref === "string" && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
              var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
              if (!didWarnAboutStringRefs[componentName]) {
                error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', getComponentNameFromType(ReactCurrentOwner.current.type), config.ref);
                didWarnAboutStringRefs[componentName] = true;
              }
            }
          }
        }
        function defineKeyPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingKey = function() {
              if (!specialPropKeyWarningShown) {
                specialPropKeyWarningShown = true;
                error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
              }
            };
            warnAboutAccessingKey.isReactWarning = true;
            Object.defineProperty(props, "key", {
              get: warnAboutAccessingKey,
              configurable: true
            });
          }
        }
        function defineRefPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingRef = function() {
              if (!specialPropRefWarningShown) {
                specialPropRefWarningShown = true;
                error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
              }
            };
            warnAboutAccessingRef.isReactWarning = true;
            Object.defineProperty(props, "ref", {
              get: warnAboutAccessingRef,
              configurable: true
            });
          }
        }
        var ReactElement = function(type, key, ref, self, source, owner, props) {
          var element = {
            // This tag allows us to uniquely identify this as a React Element
            $$typeof: REACT_ELEMENT_TYPE,
            // Built-in properties that belong on the element
            type,
            key,
            ref,
            props,
            // Record the component responsible for creating this element.
            _owner: owner
          };
          {
            element._store = {};
            Object.defineProperty(element._store, "validated", {
              configurable: false,
              enumerable: false,
              writable: true,
              value: false
            });
            Object.defineProperty(element, "_self", {
              configurable: false,
              enumerable: false,
              writable: false,
              value: self
            });
            Object.defineProperty(element, "_source", {
              configurable: false,
              enumerable: false,
              writable: false,
              value: source
            });
            if (Object.freeze) {
              Object.freeze(element.props);
              Object.freeze(element);
            }
          }
          return element;
        };
        function jsxDEV(type, config, maybeKey, source, self) {
          {
            var propName;
            var props = {};
            var key = null;
            var ref = null;
            if (maybeKey !== void 0) {
              {
                checkKeyStringCoercion(maybeKey);
              }
              key = "" + maybeKey;
            }
            if (hasValidKey(config)) {
              {
                checkKeyStringCoercion(config.key);
              }
              key = "" + config.key;
            }
            if (hasValidRef(config)) {
              ref = config.ref;
              warnIfStringRefCannotBeAutoConverted(config, self);
            }
            for (propName in config) {
              if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                props[propName] = config[propName];
              }
            }
            if (type && type.defaultProps) {
              var defaultProps = type.defaultProps;
              for (propName in defaultProps) {
                if (props[propName] === void 0) {
                  props[propName] = defaultProps[propName];
                }
              }
            }
            if (key || ref) {
              var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
              if (key) {
                defineKeyPropWarningGetter(props, displayName);
              }
              if (ref) {
                defineRefPropWarningGetter(props, displayName);
              }
            }
            return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
          }
        }
        var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
        var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement$1(element) {
          {
            if (element) {
              var owner = element._owner;
              var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
              ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
            } else {
              ReactDebugCurrentFrame$1.setExtraStackFrame(null);
            }
          }
        }
        var propTypesMisspellWarningShown;
        {
          propTypesMisspellWarningShown = false;
        }
        function isValidElement(object) {
          {
            return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
          }
        }
        function getDeclarationErrorAddendum() {
          {
            if (ReactCurrentOwner$1.current) {
              var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
              if (name) {
                return "\n\nCheck the render method of `" + name + "`.";
              }
            }
            return "";
          }
        }
        function getSourceInfoErrorAddendum(source) {
          {
            if (source !== void 0) {
              var fileName = source.fileName.replace(/^.*[\\\/]/, "");
              var lineNumber = source.lineNumber;
              return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
            }
            return "";
          }
        }
        var ownerHasKeyUseWarning = {};
        function getCurrentComponentErrorInfo(parentType) {
          {
            var info = getDeclarationErrorAddendum();
            if (!info) {
              var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
              if (parentName) {
                info = "\n\nCheck the top-level render call using <" + parentName + ">.";
              }
            }
            return info;
          }
        }
        function validateExplicitKey(element, parentType) {
          {
            if (!element._store || element._store.validated || element.key != null) {
              return;
            }
            element._store.validated = true;
            var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
            if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
              return;
            }
            ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
            var childOwner = "";
            if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) {
              childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
            }
            setCurrentlyValidatingElement$1(element);
            error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
            setCurrentlyValidatingElement$1(null);
          }
        }
        function validateChildKeys(node, parentType) {
          {
            if (typeof node !== "object") {
              return;
            }
            if (isArray(node)) {
              for (var i = 0; i < node.length; i++) {
                var child = node[i];
                if (isValidElement(child)) {
                  validateExplicitKey(child, parentType);
                }
              }
            } else if (isValidElement(node)) {
              if (node._store) {
                node._store.validated = true;
              }
            } else if (node) {
              var iteratorFn = getIteratorFn(node);
              if (typeof iteratorFn === "function") {
                if (iteratorFn !== node.entries) {
                  var iterator = iteratorFn.call(node);
                  var step;
                  while (!(step = iterator.next()).done) {
                    if (isValidElement(step.value)) {
                      validateExplicitKey(step.value, parentType);
                    }
                  }
                }
              }
            }
          }
        }
        function validatePropTypes(element) {
          {
            var type = element.type;
            if (type === null || type === void 0 || typeof type === "string") {
              return;
            }
            var propTypes;
            if (typeof type === "function") {
              propTypes = type.propTypes;
            } else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
            // Inner props are checked in the reconciler.
            type.$$typeof === REACT_MEMO_TYPE)) {
              propTypes = type.propTypes;
            } else {
              return;
            }
            if (propTypes) {
              var name = getComponentNameFromType(type);
              checkPropTypes(propTypes, element.props, "prop", name, element);
            } else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
              propTypesMisspellWarningShown = true;
              var _name = getComponentNameFromType(type);
              error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", _name || "Unknown");
            }
            if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) {
              error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
            }
          }
        }
        function validateFragmentProps(fragment) {
          {
            var keys = Object.keys(fragment.props);
            for (var i = 0; i < keys.length; i++) {
              var key = keys[i];
              if (key !== "children" && key !== "key") {
                setCurrentlyValidatingElement$1(fragment);
                error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                setCurrentlyValidatingElement$1(null);
                break;
              }
            }
            if (fragment.ref !== null) {
              setCurrentlyValidatingElement$1(fragment);
              error("Invalid attribute `ref` supplied to `React.Fragment`.");
              setCurrentlyValidatingElement$1(null);
            }
          }
        }
        var didWarnAboutKeySpread = {};
        function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
          {
            var validType = isValidElementType(type);
            if (!validType) {
              var info = "";
              if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) {
                info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
              }
              var sourceInfo = getSourceInfoErrorAddendum(source);
              if (sourceInfo) {
                info += sourceInfo;
              } else {
                info += getDeclarationErrorAddendum();
              }
              var typeString;
              if (type === null) {
                typeString = "null";
              } else if (isArray(type)) {
                typeString = "array";
              } else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
                typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
                info = " Did you accidentally export a JSX literal instead of a component?";
              } else {
                typeString = typeof type;
              }
              error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
            }
            var element = jsxDEV(type, props, key, source, self);
            if (element == null) {
              return element;
            }
            if (validType) {
              var children = props.children;
              if (children !== void 0) {
                if (isStaticChildren) {
                  if (isArray(children)) {
                    for (var i = 0; i < children.length; i++) {
                      validateChildKeys(children[i], type);
                    }
                    if (Object.freeze) {
                      Object.freeze(children);
                    }
                  } else {
                    error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
                  }
                } else {
                  validateChildKeys(children, type);
                }
              }
            }
            {
              if (hasOwnProperty.call(props, "key")) {
                var componentName = getComponentNameFromType(type);
                var keys = Object.keys(props).filter(function(k) {
                  return k !== "key";
                });
                var beforeExample = keys.length > 0 ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
                if (!didWarnAboutKeySpread[componentName + beforeExample]) {
                  var afterExample = keys.length > 0 ? "{" + keys.join(": ..., ") + ": ...}" : "{}";
                  error('A props object containing a "key" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />', beforeExample, componentName, afterExample, componentName);
                  didWarnAboutKeySpread[componentName + beforeExample] = true;
                }
              }
            }
            if (type === REACT_FRAGMENT_TYPE) {
              validateFragmentProps(element);
            } else {
              validatePropTypes(element);
            }
            return element;
          }
        }
        function jsxWithValidationStatic(type, props, key) {
          {
            return jsxWithValidation(type, props, key, true);
          }
        }
        function jsxWithValidationDynamic(type, props, key) {
          {
            return jsxWithValidation(type, props, key, false);
          }
        }
        var jsx = jsxWithValidationDynamic;
        var jsxs = jsxWithValidationStatic;
        exports.Fragment = REACT_FRAGMENT_TYPE;
        exports.jsx = jsx;
        exports.jsxs = jsxs;
      })();
    }
  }
});

// node_modules/react/jsx-runtime.js
var require_jsx_runtime = __commonJS({
  "node_modules/react/jsx-runtime.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_jsx_runtime_development();
    }
  }
});

// node_modules/@django-bridge/react/dist/index.js
var require_dist = __commonJS({
  "node_modules/@django-bridge/react/dist/index.js"(exports, module) {
    var we = Object.create;
    var V = Object.defineProperty;
    var Pe = Object.getOwnPropertyDescriptor;
    var be = Object.getOwnPropertyNames;
    var Fe = Object.getPrototypeOf;
    var Me = Object.prototype.hasOwnProperty;
    var De = (e, o) => {
      for (var t in o) V(e, t, { get: o[t], enumerable: true });
    };
    var fe = (e, o, t, n) => {
      if (o && typeof o == "object" || typeof o == "function") for (let r of be(o)) !Me.call(e, r) && r !== t && V(e, r, { get: () => o[r], enumerable: !(n = Pe(o, r)) || n.enumerable });
      return e;
    };
    var D = (e, o, t) => (t = e != null ? we(Fe(e)) : {}, fe(o || !e || !e.__esModule ? V(t, "default", { value: e, enumerable: true }) : t, e));
    var ke = (e) => fe(V({}, "__esModule", { value: true }), e);
    var Be = {};
    De(Be, { App: () => Oe, BuildLinkElement: () => se, Config: () => X, DirtyFormContext: () => F, DirtyFormMarker: () => J, Form: () => le, FormSubmissionStatus: () => Q, FormWidgetChangeNotificationContext: () => K, Link: () => Ce, MessagesContext: () => L, NavigationContext: () => k, OverlayContext: () => z, RenderFrame: () => Z, buildLinkElement: () => ie });
    module.exports = ke(Be);
    var M = D(require_react());
    var ve = D(require_lib());
    var q = class {
      constructor() {
        __publicField(this, "views");
        __publicField(this, "telepathRegistry");
        __publicField(this, "addView", (o, t) => (this.views.set(o, t), this));
        __publicField(this, "addAdapter", (o, t) => (this.telepathRegistry.register(o, t), this));
        __publicField(this, "unpack", (o) => this.telepathRegistry.unpack(o));
        this.views = /* @__PURE__ */ new Map(), this.telepathRegistry = new ve.default(), this.addAdapter("Date", Date);
      }
    };
    async function O(e, o) {
      let t, n = { "X-Requested-With": "DjangoBridge" };
      o && (n["X-DjangoBridge-Overlay"] = "true");
      try {
        t = await fetch(e, { headers: n });
      } catch {
        return { action: "network-error" };
      }
      return t.status === 500 ? { action: "server-error" } : t.headers.get("X-DjangoBridge-Action") ? t.json() : { action: "reload" };
    }
    async function te(e, o, t) {
      let n, r = { "X-Requested-With": "DjangoBridge" };
      t && (r["X-DjangoBridge-Overlay"] = "true");
      try {
        n = await fetch(e, { method: "post", headers: r, body: o });
      } catch {
        return { action: "network-error" };
      }
      return n.status === 500 ? { action: "server-error" } : n.headers.get("X-DjangoBridge-Action") ? n.json() : { action: "reload" };
    }
    var u = require_react();
    var Y = 1;
    function $(e, o, t, n, r = {}) {
      Y += 1;
      let [i, p] = (0, u.useState)({ id: Y, path: n, metadata: { title: "Loading" }, view: "loading", props: {}, context: {} }), [v, c] = (0, u.useState)(false), g = (0, u.useCallback)((s, R, P, b, N, S, U = true, ye = true) => {
        var _a;
        let de = i.id, me = P !== i.view || ye;
        if (me && (Y += 1, de = Y), !e && (document.title = R.title, U)) {
          let pe = 0, xe = window.scrollY, ge = (_a = window.history) == null ? void 0 : _a.state;
          (ge == null ? void 0 : ge.prevPath) === s && (pe = ge.prevScrollPosition ?? 0), window.history.pushState({ prevPath: window.location.pathname, prevScrollPosition: xe }, "", s), window.scrollTo(0, pe);
        }
        let ue = { id: de, path: s, metadata: R, view: P, props: b, context: N };
        p(ue), r.onNavigation && r.onNavigation(ue, me, S);
      }, [r, i.id, i.view, e]), [d, C] = (0, u.useState)(null), h = (0, u.useCallback)((s, R, P = true, b = false) => {
        if (s.action === "reload") if (!e) window.location.href = R;
        else return e.handleResponse(s, R);
        else {
          if (s.action === "redirect") return C(s.path), Promise.resolve();
          if (s.action === "render") {
            if (e && !s.overlay) return e.handleResponse(s, R);
            let N = o(s.props), S = o(s.context), U = !b;
            U && s.view === i.view && i.shouldReloadCallback && (U = i.shouldReloadCallback(R, N)), g(R, s.metadata, s.view, N, S, s.messages, P, U);
          } else if (s.action === "close-overlay") r.onOverlayClose && r.onOverlayClose(s.messages);
          else {
            if (s.action === "server-error") return r.onServerError && r.onServerError("server"), Promise.reject();
            if (s.action === "network-error") return r.onServerError && r.onServerError("network"), Promise.reject();
          }
        }
        return Promise.resolve();
      }, [r, i, e, g, o]), f = (0, u.useRef)(1), l = (0, u.useRef)(1), y = (0, u.useRef)(false), a = (0, u.useCallback)(async (s, R, P, b = false) => {
        f.current += 1;
        let N = f.current;
        y.current = true;
        let S = await s();
        N < l.current || (l.current = N, S !== null && (await h(S, R, P, b), y.current = false));
      }, [h]), m = (0, u.useCallback)((s, R = true) => {
        let P = s;
        if (!s.startsWith("/")) {
          let b = new URL(s);
          if (b.origin !== window.location.origin) return window.location.href = s, Promise.resolve();
          P = b.pathname + b.search;
        }
        return c(true), a(() => O(P, !!e), P, R).finally(() => {
          c(false);
        });
      }, [a, e]), w = (0, u.useCallback)((s, R) => {
        s === i.id && (i.path = R, e || history.replaceState({}, "", i.path));
      }, [i, e]), oe = (0, u.useCallback)((s, R) => a(() => te(s, R, !!e), s, true), [a, e]), Re = (0, u.useCallback)(() => y.current ? Promise.resolve() : a(() => O(i.path, !!e), i.path, false, true), [i.path, a, e]);
      return (0, u.useEffect)(() => {
        h(t, n, false);
      }, []), (0, u.useEffect)(() => {
        d && (C(null), m(d));
      }, [m, d]), { parent: e, currentFrame: i, isLoading: i.view === "loading", handleResponse: h, navigate: m, replacePath: w, submitForm: oe, refreshProps: Re, isNavigating: v, setIsNavigating: c };
    }
    var x = D(require_react());
    var A = require_jsx_runtime();
    var re = x.default.createContext(() => {
    });
    var F = x.default.createContext({ isDirty: false, requestUnload: () => Promise.resolve(), unloadRequested: false, unloadBlocked: false, confirmUnload: () => {
    }, cancelUnload: () => {
    }, unloadConfirmed: false });
    function ne({ handleBrowserUnload: e = false, children: o }) {
      let [t, n] = x.default.useState(false), [r, i] = x.default.useState({ cb: () => {
      } }), [p, v] = x.default.useState(false), [c, g] = x.default.useState(false), d = x.default.useRef(null), C = () => d.current && !!d.current.querySelector("div.dirty-form-marker") || false;
      x.default.useEffect(() => {
        if (e && c) {
          let y = "This page has unsaved changes.", a = (m) => C() ? (m.returnValue = y, y) : "";
          return window.addEventListener("beforeunload", a), () => {
            window.removeEventListener("beforeunload", a);
          };
        }
        return () => {
        };
      }, [e, c]);
      let h = x.default.useContext(re), f = x.default.useCallback(() => {
        g(C()), h();
      }, [h]), l = x.default.useMemo(() => ({ isDirty: c, requestUnload: () => c && C() ? (n(true), new Promise((y) => {
        i({ cb: y });
      })) : Promise.resolve(), unloadRequested: t, unloadBlocked: c && t, confirmUnload: () => {
        t && (v(true), r.cb(), n(false), i({ cb: () => {
        } }));
      }, cancelUnload: () => {
        t && (n(false), i({ cb: () => {
        } }));
      }, unloadConfirmed: p }), [c, r, p, t]);
      return (0, A.jsx)("div", { ref: d, children: (0, A.jsx)(re.Provider, { value: f, children: (0, A.jsx)(F.Provider, { value: l, children: o }) }) });
    }
    function J() {
      let e = x.default.useContext(re);
      return x.default.useEffect(e), (0, A.jsx)("div", { className: "dirty-form-marker", style: { display: "none" } });
    }
    var ae = D(require_react());
    var B = D(require_react());
    var z = B.default.createContext({ overlay: false, closeRequested: false, requestClose: () => {
      console.error("OverlayContext.requestClose() called from outside an overlay");
    }, onCloseCompleted: () => {
      console.error("OverlayContext.onCloseCompleted() called from outside an overlay");
    } });
    var k = B.default.createContext({ frameId: 0, path: "/", props: {}, context: {}, navigate: () => (console.error("navigate() called from outside a Django Bridge Browser"), Promise.resolve()), replacePath: () => {
      console.error("replacePath() called from outside a Django Bridge Browser");
    }, submitForm: () => (console.error("submitForm() called from outside a Django Bridge Browser"), Promise.resolve()), openOverlay: () => {
      throw console.error("openOverlay() called from outside a Django Bridge Browser"), new Error("Modal cannot be opened here");
    }, refreshProps: () => (console.error("refreshProps() called from outside a Django Bridge Browser"), Promise.resolve()), isNavigating: false });
    var K = B.default.createContext(() => {
    });
    var Q = B.default.createContext(false);
    var L = B.default.createContext({ messages: [], pushMessage: () => {
    } });
    var j = require_jsx_runtime();
    function Ne({ config: e, frame: o }) {
      let t = e.views.get(o.view);
      if (!t) return (0, j.jsxs)("p", { children: ["Unknown view '", o.view, "'"] });
      let n = (0, j.jsx)(t, { ...o.props });
      return e.contextProviders.forEach((r, i) => {
        n = (0, j.jsx)(r.Provider, { value: o.context[i], children: n });
      }), (0, j.jsx)("div", { children: n }, o.id);
    }
    var Z = Ne;
    var _ = require_jsx_runtime();
    function Ee({ config: e, navigationController: o, openOverlay: t }) {
      let { currentFrame: n, navigate: r, replacePath: i, submitForm: p, refreshProps: v, isNavigating: c } = o, { isDirty: g, requestUnload: d, cancelUnload: C } = ae.default.useContext(F), h = ae.default.useMemo(() => ({ frameId: n.id, path: n.path, props: n.props, context: n.context, navigate: (f, l = {}) => !g || l.skipDirtyFormCheck === true ? (l.skipDirtyFormCheck === true && C(), r(f, l.pushState)) : d().then(() => r(f, l.pushState)), replacePath: i, submitForm: p, openOverlay: t, refreshProps: v, isNavigating: c }), [n, i, p, t, g, d, C, r, v, c]);
      return (0, _.jsx)(k.Provider, { value: h, children: (0, _.jsx)("div", { children: (0, _.jsx)(Z, { config: e, frame: n }) }, n.id) });
    }
    var ee = Ee;
    var W = D(require_react());
    var he = require_jsx_runtime();
    function ie({ children: e, href: o, skipDirtyFormCheck: t = false, ...n }, { navigate: r }, i) {
      return (0, he.jsx)("a", { onClick: (v) => {
        o && (v.preventDefault(), r(o, { skipDirtyFormCheck: t }));
      }, href: o || "#", ref: i, ...n, children: e });
    }
    var se = W.default.createContext(ie);
    var Se = W.default.forwardRef((e, o) => {
      let t = W.default.useContext(k);
      return W.default.useContext(se)(e, t, o);
    });
    var Ce = Se;
    var X = class extends q {
      constructor() {
        super();
        __publicField(this, "contextProviders");
        __publicField(this, "addContextProvider", (o, t) => (this.contextProviders.set(o, t), this));
        this.contextProviders = /* @__PURE__ */ new Map();
      }
    };
    var T = D(require_react());
    var I = require_jsx_runtime();
    function le({ children: e, onSubmit: o, isDirty: t = false, disableDirtyCheck: n = false, ...r }) {
      let { submitForm: i, navigate: p } = T.default.useContext(k), [v, c] = T.default.useState(false), [g, d] = T.default.useState(t), { cancelUnload: C } = T.default.useContext(F), h = (l) => {
        if (g && C(), !(o && (o(l), l.defaultPrevented)) && l.target instanceof HTMLFormElement) {
          if (v) {
            l.preventDefault();
            return;
          }
          let y = new FormData(l.target);
          if (l.nativeEvent instanceof SubmitEvent && l.nativeEvent.submitter) {
            let { submitter: a } = l.nativeEvent;
            (a instanceof HTMLButtonElement || a instanceof HTMLInputElement) && a.name && a.value && y.set(a.name, a.value);
          }
          if (l.target.method === "post") l.preventDefault(), c(true), i(l.target.action, y).catch(() => c(false));
          else if (l.target.method === "get") {
            l.preventDefault();
            let a = Array.from(y.entries()).map((w) => `${encodeURIComponent(w[0])}=${encodeURIComponent(w[1])}`).join("&"), m = l.target.action + (l.target.action.indexOf("?") === -1 ? "?" : "&") + a;
            c(true), p(m).catch(() => c(false));
          }
        }
      }, f = T.default.useCallback(() => {
        d(true);
      }, []);
      return (0, I.jsxs)("form", { onSubmit: h, ...r, children: [g && !n && (0, I.jsx)(J, {}), (0, I.jsx)(Q.Provider, { value: v, children: (0, I.jsx)(K.Provider, { value: f, children: e }) })] });
    }
    var G = D(require_react());
    var H = require_jsx_runtime();
    function ce({ config: e, initialResponse: o, initialPath: t, parentNavigationContoller: n, render: r, requestClose: i, closeRequested: p, onCloseCompleted: v, onServerError: c }) {
      let { pushMessage: g } = G.default.useContext(L), d = $(n, e.unpack, o, t, { onNavigation: (l, y, a) => {
        a.forEach(g);
      }, onOverlayClose: (l) => {
        l.forEach(g), i();
      }, onServerError: c }), C = G.default.useContext(F), h = G.default.useCallback(({ skipDirtyFormCheck: l = false } = {}) => {
        !l && C.isDirty ? C.requestUnload().then(() => i()) : i();
      }, [C, i]), f = G.default.useMemo(() => ({ overlay: true, closeRequested: p, requestClose: h, onCloseCompleted: v }), [p, v, h]);
      return d.isLoading ? (0, H.jsx)(H.Fragment, {}) : (0, H.jsx)(z.Provider, { value: f, children: r((0, H.jsx)(ee, { config: e, navigationController: d, openOverlay: () => {
      } })) });
    }
    var E = require_jsx_runtime();
    function Oe({ config: e, initialResponse: o }) {
      let [t, n] = M.default.useReducer((a, m) => {
        switch (m.action) {
          case "push":
            return a.concat([m.message]);
          case "clear":
            return [];
          default:
            return a;
        }
      }, []), r = M.default.useCallback((a) => {
        n({ action: "push", message: a });
      }, [n]), i = M.default.useCallback((a) => {
        a === "server" ? r({ level: "error", text: "A server error occurred. Please try again later." }) : a === "network" && r({ level: "error", text: "A network error occurred. Please check your internet connection or try again later." });
      }, [r]), [p, v] = M.default.useState(null), [c, g] = M.default.useState(false), d = M.default.useRef(null), C = (a, m, w) => {
        m && (g(true), d.current = null), m && n({ action: "clear" }), w.forEach(r);
      }, h = window.location.pathname + window.location.search + window.location.hash, f = $(null, e.unpack, o, h, { onNavigation: C, onServerError: i });
      M.default.useEffect(() => {
        let a = document.querySelector(".django-bridge-load");
        a instanceof HTMLElement && (a.classList.add("django-bridge-load--hidden"), setTimeout(() => {
          a.remove();
        }, 200));
        let m = () => {
          f.navigate(document.location.pathname, false);
        };
        return window.addEventListener("popstate", m), () => {
          window.removeEventListener("popstate", m);
        };
      }, []);
      let l = async (a, m, { onClose: w } = {}) => {
        f.setIsNavigating(true);
        let oe = await O(a, true);
        w && (d.current = w), g(false), v({ render: m, initialResponse: oe, initialPath: a }), f.setIsNavigating(false);
      }, y = M.default.useMemo(() => ({ messages: t, pushMessage: r }), [t, r]);
      return (0, E.jsx)(ne, { handleBrowserUnload: true, children: (0, E.jsxs)(L.Provider, { value: y, children: [p && (0, E.jsx)(ne, { children: (0, E.jsx)(ce, { config: e, initialResponse: p.initialResponse, initialPath: p.initialPath, parentNavigationContoller: f, render: (a) => p.render(a), requestClose: () => g(true), closeRequested: c, onCloseCompleted: () => {
        v(null), g(false), d.current && (d.current(), d.current = null);
      }, onServerError: i }) }), !f.isLoading && (0, E.jsx)(ee, { config: e, navigationController: f, openOverlay: (a, m, w) => void l(a, m, w) })] }) });
    }
  }
});
export default require_dist();
/*! Bundled license information:

react/cjs/react-jsx-runtime.development.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=@django-bridge_react.js.map
